dbmikus.com
===========

Setup and Installation
----------------------

Make sure you have NPM installed and the Gatsby CLI installed, and then install
your NPM dependencies:


```
npm install -g gatsby-cli
npm ci

# or for production when you want to ignore dev dependencies
npm ci --production
```


